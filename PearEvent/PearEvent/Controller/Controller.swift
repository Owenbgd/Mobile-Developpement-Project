//
//  Controller.swift
//  PearEvent
//
//  Created by goldorak on 07/02/2022.
//
import UIKit
import Foundation
/*
let requestFactory = RequestFactory()

requestFactory.getInformationList { (errorHandle, information) in
    if let _ = errorHandle.errorType, let errorMessage =
     errorHandle.errorMessage {
        print(errorMessage)
    }
    else if let list = information, let information = list.last {
        print(information.id)
    }
    else {
        print("Houston we got a problem")
    }
}
requestFactory.deleteInformation(with: "recoyHw0KB4S6hta9") { (errorHandle, response) in
    if let _ = errorHandle.errorType, let errorMessage =
     errorHandle.errorMessage {
        print(errorMessage)
    }
    else if let reponse = response {
        print(reponse.deleted)
    }
    else {
        print("Houston we got a problem")
    }
}
*/
